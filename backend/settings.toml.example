# === DB ===
DB_PATH = "data/screening.db"

# === 텔레그램 ===
TELEGRAM_BOT_TOKEN = ""
TELEGRAM_CHAT_ID = ""

# === 거래량 조건 (기본값 변경 시에만 주석 해제) ===
# VOLUME_MIN = 30000
# VOLUME_RATIO = 1.5
# VOLUME_MA_PERIOD = 5

# === 이평선 추세 (기본값 변경 시에만 주석 해제) ===
# TREND_PERIODS = [20, 60, 120]
# TREND_MIN_COUNT = 2

# === 골든크로스 (기본값 변경 시에만 주석 해제) ===
# GOLDEN_CROSS_PERIODS = [3, 5, 10]

# === 수급 (기본값 변경 시에만 주석 해제) ===
# SUPPLY_DEMAND_PERIODS = [2, 5, 20]

# === 리포트 ===
REPORT_BASE_URL = "http://localhost:8000"

# === AI 요약 ===
AI_PROVIDER = "gemini"
AI_API_KEY = ""
AI_MODEL = "gemini-3-flash-preview"
TAVILY_API_KEY = ""
AI_SUMMARY_TOP_N = 5
